<h1>Actor #<%= @background_actor.id%> details</h1>

<a href="/actors">go back</a>
<hr>
 <dl>
  <dt>Name</dt>
  <dd><%=@background_actor.name%></dd>
  <dt>Dob</dt>
  <dd><%=@background_actor.dob%></dd>
  <dt>Bio</dt>
  <dd><%=@background_actor.bio%></dd>
  <dt>Image</dt>
  <dd><img src="http://robohash.org/<%= @background_actor.id%>"></td></dd>
  <dt>Bio</dt>
  <dd><%=time_ago_in_words(@background_actor.created_at)%></dd>
  <dt>Bio</dt>
  <dd><%=time_ago_in_words(@background_actor.updated_at)%></dd>
</dl>

<h2>Filmography</h2>

<table border ="1">
    <td>Title</td>
    <td>Year</td>
    <td>Director</td>
    <td>Character name</td>
    <td></td>
  </tr>
<% Character.all.each do |display| %>


  <%if display.actor_id == @background_actor.id%>
    <tr>
  <tr>
    <td> <%= Movie.all.where({:id => display.movie_id}).first.title%></td>
    <td>  <%=Director.all.where({:id => Movie.all.where({:id => display.movie_id}).first.director_id}).first.name%></td>
    <td> <%= display.name%></td>
    <td> <a href="/movies/<%=display.id%>">Show details</a>
  </tr>
  <%else%>
  <%end%>
<% end %>
</table>
